//- extends layout

//- block content
//-   h1= title
//-   p love to play with kids
//-   <button type="button" class="btn btn-secondary">Press Me</button>
//-   div#faster Increase the speed 
//-   button(type="button" class="btn btn-danger" onclick="goFaster()") Go Faster
//-   script. 
//-     let mph = 20;
//-     let change = 1;
//-     function goFaster() {
//-       mph = mph + change;
//-       change = change + 1;
//-       document.getElementById("faster").innerText = `MPH ${mph} with change ${change}`
//-     }
 extends layout

block content
  if (!user)
    a(href="/login") Login
    br
    a(href="/register") Register
  if (user)
    p You are currently logged in as #{user.username}
    a(href="/butterfly") Take me to butterfly 
    br
    a(href="/logout") Logout
